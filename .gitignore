# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Personal signing team for device builds — see signing.xcconfig for the shape.
signing.local.xcconfig

# Generated by XcodeGen from project.yml — run `xcodegen generate`
*.xcodeproj
# Also generated by XcodeGen, from each target's `entitlements.properties` and
# `info.properties` blocks in project.yml. The spec is the source of truth; a
# committed copy would be a second one, free to drift from it.
*.entitlements
App/*/Info.plist

# macOS
.DS_Store

# Local scratch
/scratch/
